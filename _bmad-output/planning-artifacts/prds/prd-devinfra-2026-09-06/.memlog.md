---
topic: devinfra — local development infrastructure stack
updated: 2026-09-06T18:42
---

- (decision) Stakes calibrated as personal/hobby — PRD kept short (~2 pages), light on governance/metrics rigor
- (decision) Working mode: Fast path — batch remaining gaps into consolidated questions, then draft full PRD with [ASSUMPTION] tags
- (assumption) Working thesis for Vision: devinfra is a curated, persistent, loopback-bound backing-service plane that is verified working (make up && make smoke), NOT a dev-env orchestrator (Tilt/Skaffold/Coder) or ephemeral per-test containers (Testcontainers)
- (event) Brownfield analysis complete: compose.yaml (13 services, 2 profiles), Makefile (23 targets), smoke-test.sh (360 lines, functional verification not port checks), README gotchas section identified as captured expertise
- (decision) All four enhancement directions in scope: more services, modular a-la-carte, batteries in the box, repo hygiene/durability
- (decision) Consumption model (clone-standalone vs reusable base vs CLI) deliberately left OPEN — PRD must frame it as an explicit open question with costs per path, not decide it
- (decision) No current pain — PRD leads with reach/ambition, not repair. Stack is considered working today.
- (decision) Secrets management: OpenBao, NOT HashiCorp Vault (Vault relicensed BUSL; OpenBao is the LF/OSS fork). User-stated constraint.
- (event) Landscape research digest received: closest comparables are Testcontainers Desktop, Spring Boot docker-compose module, .NET Aspire, devenv.sh, LocalStack, Supabase CLI, grafana/otel-lgtm, docker/awesome-compose
- (decision) Positioning whitespace confirmed by research: language-agnostic + project-agnostic + long-lived persistent stack; pre-seeded versioned Keycloak realm is the highest-friction least-available piece; functional smoke test (not ps checks) is rare in template repos
- (assumption) grafana/otel-lgtm (single image packing Collector+Prom+Loki+Tempo+Grafana) is a direct alternative to the 5-container observability profile — recorded in addendum as a considered alternative for the architect
- (assumption) Compose 'include' (available since Compose 2.20) is the intended mechanism for the modular a-la-carte direction
- (assumption) Keycloak 24+ may support --override on realm import, which would remove the destructive-reimport gotcha documented in README. UNVERIFIED — flagged as an open question, not asserted.
- (decision) User raised pixi-vs-Makefile as task runner. Recorded as PRD open question Q8 + addendum section F rather than decided in the PRD (mechanism choice = architecture decision)
- (event) Concrete defect found grounding Q8: make lint silently skips shellcheck when absent and skips YAML validation when PyYAML absent, reporting success for checks never run. Unmanaged tool dependencies undermine FR-16.
- (event) Makefile lint target invokes bare python3 twice, violating the user's global pixi-only-runner standard
- (assumption) Recommended shape: pixi owns task surface + provisions tools from conda-forge (shellcheck, yamllint, python, jq); non-trivial shell logic (wait loop, destroy confirmation, urls printf) moves from Makefile into scripts/*.sh invoked by pixi tasks. Not confirmed by user.
- (decision) MVP sequencing CONFIRMED by user: CI/durability -> modularity -> batteries -> catalog expansion. PRD Assumption 8 resolved.
- (event) VERIFIED against running quay.io/keycloak/keycloak:26.4.0 binary: 'kc.sh import' supports --override <true|false>, DEFAULT TRUE. Existing realms ARE overwritten. Open question Q2 resolved affirmatively.
- (decision) Makefile target keycloak-reimport (drops the keycloak Postgres database) is unnecessary on KC 26.4 and should be replaced with a non-destructive kc.sh import. Corresponding README Gotcha is obsolete.
- (assumption) UNRESOLVED sub-question: whether a running Keycloak server invalidates its Infinispan realm cache after an out-of-band kc.sh import, or requires a container restart. kc.sh export against a live container is already proven to work (existing Makefile target).
- (event) EMPIRICALLY VERIFIED on live KC 26.4.0 via throwaway probe realm: kc.sh import --override (default true) DOES overwrite an existing realm in the database, but the RUNNING server keeps serving stale Infinispan-cached realm data. DB showed PROBE-V2 while the admin API returned PROBE-V1. A container restart applies it.
- (event) SECOND VERIFIED GOTCHA: kc.sh import run via 'docker compose exec' against a running container imports successfully but then exits NON-ZERO trying to bind the management interface on 9000 (already held by the running server). Workaround verified: --http-management-port <free port> makes it exit 0.
- (decision) Non-destructive realm reimport is: kc.sh import --file <f> --optimized --http-management-port 9999, then docker compose restart keycloak. Replaces the DB-dropping make keycloak-reimport target. Verified end to end; probe realm deleted, devinfra realm intact.
- (event) LocalStack MATERIAL CHANGE verified: Community Edition discontinued 2026-03-23; GitHub repo archived read-only; unified image now requires LOCALSTACK_AUTH_TOKEN; free Hobby tier is explicitly NON-COMMERCIAL only; paid from 39 USD/user/month. Requires Docker socket mount. Podman support experimental.
- (decision) FR-10 (cloud emulation) re-scoped away from LocalStack. Recommended substitute: motoserver (Apache-2.0, broad AWS coverage) + ElasticMQ (Apache-2.0, SQS), keeping MinIO as the S3 endpoint. Avoids auth token, non-commercial restriction, and Docker socket mount.
- (event) OpenBao VERIFIED (agent ran the image): official first-party image openbao/openbao (also quay.io + ghcr.io), latest stable 2.6.2 (2026-08-18), alpine base, non-root, MPL-2.0, Linux Foundation/OpenSSF governed.
- (event) OpenBao dev mode verified: default CMD is already 'server -dev'; entrypoint injects -dev-root-token-id from BAO_DEV_ROOT_TOKEN_ID and -dev-listen-address from BAO_DEV_LISTEN_ADDRESS. Starts initialized+unsealed with zero manual steps. Satisfies FR-9 directly.
- (event) OpenBao healthcheck: GET /v1/sys/health returns 200 in dev mode. Image ships BusyBox wget AND nc (unlike Keycloak) so no /dev/tcp fallback needed. KV v2 mounted at secret/ by default in dev mode - no enable step for the smoke test.
- (decision) OpenBao port 8200 confirmed free of conflict with every currently allocated devinfra port. CLI binary is 'bao' with a vault symlink; client env vars BAO_ADDR/BAO_TOKEN with VAULT_ fallback.
- (event) CORRECTION to earlier entry, verified in KC 26.4 source (ImportUtils.java:115): --override true performs removeRealm() then re-import. The TARGET realm is destroyed and recreated, so runtime state not present in the JSON is lost for that realm. Other realms and the database survive. It is NOT a merge.
- (event) Verified in KC 26.4 source: start/start-dev --import-realm hard-codes Strategy.IGNORE_EXISTING (ExportImportManager.java:78-79); logs 'Realm already exists. Import skipped'. --override is an import-command-only option (AbstractAutoBuildCommand excludes OptionCategory.IMPORT from start commands), default true, introduced in KC 21.1.0.
- (event) Verified: there is NO KC_IMPORT_REALM env var, and KC_OVERRIDE in a compose environment: block does NOT affect start-dev --import-realm. Keycloak docs state all nodes should be STOPPED before an override import because the import command does not attach to the cache cluster - this is the documented mechanism behind the stale-cache divergence measured empirically.
- (decision) keycloak-config-cli (adorsys, Apache-2.0, pure Admin REST API, supports KC 22-26.x with explicit 26.4.0 baselines) identified as the genuinely idempotent realm-as-code option for Docker Compose. Recorded as the preferred path over kc.sh import for FR-18/realm management; architect to choose.
- (event) POST /admin/realms/{realm}/partialImport exists in 26.4 with ifResourceExists SKIP|OVERWRITE|FAIL (default FAIL) but covers ONLY clients, roles, identity providers+mappers, groups, users - NOT realm-level settings. Keycloak Operator realm-import CRD is Kubernetes-only and creation-only; irrelevant to Compose.
- (event) Reviewer gate run (rubric walker). Verdict: strong for stakes. 0 critical / 4 high / 12 medium / 4 low. Dimensions: decision-readiness strong, substance strong, strategic coherence strong, done-ness adequate, scope honesty strong, downstream usability THIN, shape fit strong. Full review at review-rubric.md
- (change) All 20 review findings resolved. Highs: removed LocalStack from 4.2 candidate list; FR-19 now requires the Smoke Test to actually pass on a non-Docker runtime rather than be documented; FR-16 hoisted the 15-minute bound out of the assumption tag; 6.1 step 1 now flags its gating on Q8.
- (change) Mediums: FR-6 completeness contract reconciled to five declaration-based items; FR-7 noeviction bullet made observable; FR-11 demoted to a Catalog backlog note and the ID reserved; FR-13 check strengthened to per-signal panel rendering; FR-18 given ownership of the make keycloak-reimport behavior change; NFRs numbered NFR-1..NFR-8; Profile-as-Selection glossary drift fixed; UJs tagged EXISTING/NEW; UJ-1 two-minute claim reconciled with NFR-3 warm-cache qualifier.
- (change) Lows and mechanical: NFR-6 configuration honesty made checkable via docker compose config assertion; FR-14 documentation statement moved to the Description; corrected 420->424 compose lines, the fourth profile combination, smoke-test skipping per Service not Profile, make urls overstatement, and the section 0 port-allocation claim; fixed Q8 citing addendum section F instead of E; condensed the Q2 altitude leak and pointed it at addendum section D.
- (event) PRD finalized
